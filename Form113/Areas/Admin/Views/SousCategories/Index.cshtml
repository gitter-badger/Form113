@model IEnumerable<DataLayer.Models.SousCategories>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categories.Libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categories.Libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSousCategorie }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSousCategorie }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSousCategorie })
        </td>
    </tr>
}

</table>
